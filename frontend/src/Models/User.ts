export type UserProfileToken = {
    userName: string;
    email: string;
    token: string;
};

export type UserProfile = {
    userName: string;
    email: string;
};

export type UserProfileHandling = {
    id: string;
    userName: string;
    email: string;
    firstName: string;
    lastName: string;
};